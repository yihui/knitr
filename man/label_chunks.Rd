% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parser.R
\name{label_chunks}
\alias{label_chunks}
\title{Assign labels to unnamed code chunks in a document}
\usage{
label_chunks(
  input,
  text = NULL,
  output = NULL,
  label = NULL,
  prefix = opts_knit$get("unnamed.chunk.label")
)
}
\arguments{
\item{input}{File path to the document with code chunks to convert.}

\item{text}{A character vector of the document content. If provided, \code{input}
is ignored, and the syntax is detected from the content.}

\item{output}{The default \code{NULL} will output to console. Other values
can be a file path to write the converted content into or a function which
takes \code{input} as argument and returns a file path to write into (e.g.,
\code{output = identity} to overwrite the input file).}

\item{label}{A function to generate a label for an unnamed chunk. It is
called with two arguments: the chunk's options (a list, without the
\code{label}), and the index of the unnamed chunk in the document (an integer
starting from 1). It should return a character string. The default function
generates labels of the form \samp{prefix-i} using the \code{prefix} argument.
If a generated label happens to be already in use, a numeric suffix is
appended to make it unique.}

\item{prefix}{The prefix for the default \code{label} function. Defaults to the
global option \code{unnamed.chunk.label} (see \link{opts_knit}), i.e.,
\samp{unnamed-chunk}.}
}
\value{
A character vector of the document with labels assigned when \code{output = NULL}, otherwise the output file path (invisibly), with the labeled
content written to it.
}
\description{
Find code chunks that do not have a label and insert a generated label into
their chunk headers. This can be useful because \pkg{knitr} refers to
unnamed chunks by an automatically generated label like \samp{unnamed-chunk-1}
in messages and warnings, which does not tell you much about where the chunk
is. Giving each chunk an explicit label makes such messages more informative,
and also makes the cache and figure file names more meaningful.
}
\details{
The document syntax is detected automatically, and all syntaxes that support
chunk headers are supported (e.g., R Markdown \verb{```\{r\}}, R Sweave
\verb{<<>>=}, and R HTML). Existing labels are left untouched, and the labels
of other chunks are taken into account so that the newly generated labels do
not clash with them.
}
\examples{
# a document with two unnamed chunks and one named chunk
doc = c("```{r}", "1 + 1", "```", "", "```{r, echo=FALSE}", "plot(cars)", "```",
    "", "```{r named}", "summary(cars)", "```")
cat(knitr::label_chunks(text = doc), sep = "\n")

# use a custom label function based on the chunk options
cat(knitr::label_chunks(text = doc, label = function(options, i) paste0("fig", i)),
    sep = "\n")
}
\seealso{
\code{\link[=convert_chunk_header]{convert_chunk_header()}}
}
