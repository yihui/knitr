% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output.R
\name{fig_alt}
\alias{fig_alt}
\title{Retrieve the default alternative text of a plot object}
\usage{
fig_alt(x, ...)
}
\arguments{
\item{x}{A plot object.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A character string (the alt text), or \code{NULL} if none is available.
}
\description{
This S3 generic function returns the alternative text (alt text) associated
with a plot object, which \pkg{knitr} uses as the default value of the chunk
option \code{fig.alt} for the corresponding figure when the user has not provided
the alt text explicitly.
}
\details{
The default method returns \code{NULL} (i.e., no alt text). Package authors can
define methods for their plot classes so that alt text stored in a plot
object can be automatically picked up by \pkg{knitr}. For example, \pkg{knitr}
registers a method for \pkg{ggplot2} plots that returns
\code{ggplot2::get_alt_text(x)}, so that alt text set via
\code{ggplot2::labs(alt = ...)} becomes the figure's alt text without the need to
set \code{fig.alt} in the chunk.

When a chunk produces multiple plots, \code{fig.alt} is a vector and each element
corresponds to one plot (in the order the plots are printed). An explicitly
provided \code{fig.alt} takes precedence; you can also mix explicit and default
values by using \code{NA} for the plots whose alt text should come from
\code{fig_alt()}, e.g., \code{fig.alt = c(NA, "custom alt text")}.
}
\examples{
# the default method returns NULL
fig_alt(1:10)

# a package could register a method for its plot class, e.g.:
# registerS3method('fig_alt', 'ggplot', function(x, ...)
# ggplot2::get_alt_text(x))
}
