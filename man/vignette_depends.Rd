% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-vignettes.R
\name{vignette_depends}
\alias{vignette_depends}
\title{Skip vignette evaluation when declared dependencies are unavailable}
\usage{
vignette_depends()
}
\value{
The character vector of missing packages (if any), invisibly. As a
side effect, \code{eval = FALSE} is set globally for subsequent chunks when the
vector is non-empty.
}
\description{
A vignette may illustrate the use of a package in combination with other
packages that are only \samp{Suggests}ed and could be unavailable when the
vignette is built (e.g., on CRAN). If these packages are declared via
\samp{\\VignetteDepends{}} in the vignette, calling \code{vignette_depends()} in
the first code chunk sets the chunk option \code{eval = FALSE} (via
\link{opts_chunk}) when any of them is not installed, so that the rest of the
vignette is not evaluated (but still shown).
}
\details{
The dependencies are read from the current input document (see
\code{\link[=current_input]{current_input()}}) with \code{\link[tools:vignetteInfo]{tools::vignetteInfo()}}, so this function only makes
sense when called while a vignette is being built.
}
\examples{
# in the setup chunk of a vignette that has \VignetteDepends{pkgA, pkgB}:
# knitr::vignette_depends()
}
