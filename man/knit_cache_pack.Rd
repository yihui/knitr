% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{knit_cache_pack}
\alias{knit_cache_pack}
\alias{knit_cache_unpack}
\title{Customize how objects are cached}
\usage{
knit_cache_pack(x, ...)

knit_cache_unpack(x, ...)
}
\arguments{
\item{x}{The object to be packed (before caching) or unpacked (after loading
from the cache).}

\item{...}{Additional arguments (currently unused; reserved for future use).}
}
\value{
The packed or unpacked object.
}
\description{
Some objects cannot be cached and restored correctly by the default method
(which serializes them to \file{.rds} files via \code{\link[=saveRDS]{saveRDS()}} and reads them
back via \code{\link[=readRDS]{readRDS()}}), typically because they contain external pointers,
e.g., objects from the \pkg{terra} package. For these objects, you can define
S3 methods for the generic functions \code{knit_cache_pack()} and
\code{knit_cache_unpack()}: \code{knit_cache_pack()} is called on each object before it
is written to the cache, and should return a serializable version of the
object; \code{knit_cache_unpack()} is called on the object after it is read from
the cache, and should restore the original object. The default methods return
the object unchanged.
}
\details{
Since \code{knit_cache_pack()} dispatches on the live (in-memory) object and
\code{knit_cache_unpack()} dispatches on the packed (deserialized) object, the
packed object should have a different class so that the correct
\code{knit_cache_unpack()} method can be found (this is naturally the case for
\pkg{terra}, whose \code{wrap()} turns a \code{SpatRaster} into a \code{PackedSpatRaster}).
Package authors can register these methods dynamically in their \code{.onLoad()}
(see the examples), so users do not need to configure anything.
}
\examples{
# e.g., the terra package could register these methods in its .onLoad():
# registerS3method(
#   'knit_cache_pack', 'SpatRaster', function(x, ...) terra::wrap(x),
#   envir = asNamespace('knitr')
# )
# registerS3method(
#   'knit_cache_unpack', 'PackedSpatRaster', function(x, ...) terra::unwrap(x),
#   envir = asNamespace('knitr')
# )
}
