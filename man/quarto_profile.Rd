% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{quarto_profile}
\alias{quarto_profile}
\title{Get or check the active Quarto profile}
\usage{
quarto_profile(profile)
}
\arguments{
\item{profile}{An optional character vector of profile names to check against
the active profiles. If not provided, the active profile names are returned.}
}
\value{
If \code{profile} is not provided, a character vector of the active profile
names (or an empty vector when no profile is active). Otherwise, a logical
value indicating whether any of the given names is among the active
profiles.
}
\description{
When a document is rendered by Quarto, the active
\href{https://quarto.org/docs/projects/profiles.html}{profile} names are stored in
the environment variable \env{QUARTO_PROFILE} (multiple profiles are separated
by commas). This function reads that variable so that you can make chunk
output conditional on the profile, e.g., set the chunk option \code{eval} or
\code{include} to \code{quarto_profile('production')}.
}
\examples{
# the active profiles
knitr::quarto_profile()
# whether the 'production' profile is active
knitr::quarto_profile("production")
}
