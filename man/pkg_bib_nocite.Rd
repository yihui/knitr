% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/citation.R
\name{pkg_bib_nocite}
\alias{pkg_bib_nocite}
\title{Get the citation keys of R packages for a \samp{nocite} bibliography}
\usage{
pkg_bib_nocite(..., file = "")
}
\arguments{
\item{...}{Arguments passed to \code{\link[=write_bib]{write_bib()}}.}

\item{file}{The \code{.bib} file to write the bibliography database to (passed to
\code{\link[=write_bib]{write_bib()}} as its \code{file} argument). Defaults to an empty string, which
means the entries are discarded and only the keys are returned; normally
you want to set it to a \code{.bib} file that is also declared in the
\code{bibliography} field of the YAML metadata, so that the entries are actually
available to be cited.}
}
\value{
A character string of the citation keys concatenated by \samp{, },
with each key prefixed by \samp{@}, e.g., \samp{@R-knitr, @R-xfun}. This
is the syntax expected in the \code{nocite} field.
}
\description{
This function returns the citation keys of the BibTeX entries generated by
\code{\link[=write_bib]{write_bib()}}, which can be used in the \code{nocite} field of the YAML metadata
of an R Markdown document to include package citations in the References
without having to cite them explicitly in the text. Unlike \code{\link[=write_bib]{write_bib()}},
which auto-generates a fixed key (e.g., \samp{R-knitr}) for a package's
\code{Manual} citation, some packages provide additional preferred citations with
their own keys, and all of these keys are captured here.
}
\examples{
# write the entries to a .bib file and get the keys for nocite
bib = tempfile(fileext = ".bib")
knitr::pkg_bib_nocite("knitr", file = bib)
# in an R Markdown document, you could use it in the YAML metadata like this:
# --- bibliography: packages.bib nocite: '`r knitr::pkg_bib_nocite(c('knitr',
# 'rmarkdown'), file = 'packages.bib')`' ---
}
