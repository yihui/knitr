% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{process_cache}
\alias{process_cache}
\title{Customize how objects are cached}
\usage{
process_cache(x, pack = TRUE, ...)
}
\arguments{
\item{x}{The object to be packed (before caching) or unpacked (after loading
from the cache).}

\item{pack}{Whether the object is being packed (\code{TRUE}, before writing to the
cache) or unpacked (\code{FALSE}, after reading from the cache).}

\item{...}{Additional arguments (currently unused; reserved for future use).}
}
\value{
The packed or unpacked object.
}
\description{
Some objects cannot be cached and restored correctly by the default method
(which serializes them to \file{.rds} files via \code{\link[=saveRDS]{saveRDS()}} and reads them
back via \code{\link[=readRDS]{readRDS()}}), typically because they contain external pointers,
e.g., objects from the \pkg{terra} package. For these objects, you can define
S3 methods for the generic function \code{process_cache()}, which is called on
each object both before it is written to the cache (with \code{pack = TRUE}) and
after it is read from the cache (with \code{pack = FALSE}). When packing, the
method should return a serializable version of the object; when unpacking, it
should restore the original object. The default method returns the object
unchanged.
}
\details{
Since \code{process_cache()} dispatches on the object's class, and packing and
unpacking are handled by the same generic, the packed object should have a
different class from the original so that the correct method is found in each
direction (this is naturally the case for \pkg{terra}, whose \code{wrap()} turns a
\code{SpatRaster} into a \code{PackedSpatRaster}). Package authors can register these
methods dynamically in their \code{.onLoad()} (see the examples), so users do not
need to configure anything.
}
\examples{
# e.g., the terra package could register these methods in its .onLoad():
# registerS3method( 'process_cache', 'SpatRaster', function(x, pack = TRUE,
# ...) if (pack) terra::wrap(x) else x, envir = asNamespace('knitr') )
# registerS3method( 'process_cache', 'PackedSpatRaster', function(x, pack =
# TRUE, ...) if (pack) x else terra::unwrap(x), envir = asNamespace('knitr') )
}
